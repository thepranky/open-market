# DDR-G: Web frontend

**Status:** draft | **Date:**

## Before you start

Read/trace:
- `apps/web/src/app/` — page routes (layout, explore, graph, cases, jurisdictions, screen)
- `apps/web/src/lib/api.ts`, `types.ts`
- `apps/web/src/components/NavBar.tsx`
- Key components: `Evidence.tsx`, `SemanticCaseCard.tsx`, `VerificationBadges.tsx`

Run: `npm run dev`; click through all nav routes.

## Agent prompt

> Map the Next.js frontend: every route, what API it calls, shared vs product-specific components. Explain `lib/api.ts` structure and type sharing. How do case research and screening appear in nav/UX? Compare feature-folder split vs current layout. What's missing for production UX (loading, errors, auth shell)?

---

## What it does

## Why this way

## Alternatives considered

## Gaps

## Next steps
