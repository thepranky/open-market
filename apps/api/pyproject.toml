[tool.ruff]
target-version = "py310"

[tool.ruff.lint.per-file-ignores]
# Scripts and tests bootstrap sys.path before importing app modules; the imports
# are intentionally not at the top of the file.
"scripts/**" = ["E402"]
"tests/**" = ["E402"]
